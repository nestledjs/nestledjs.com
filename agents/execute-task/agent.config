{
  "name": "Coder (from task)",
  "context": "coderepos",
  "description": "Compiles task title, description, links, and attachments into a spec and fires it directly at claude --remote. No local intermediary \u2014 TaskOS runs the command itself.",
  "command": "cd \"$([ -d \"$HOME/workspaces/nestledjs.com\" ] && printf %s \"$HOME/workspaces/nestledjs.com\" || printf %s \"$HOME/IdeaProjects/nestledjs.com\")\" && flightdesk register --project 443ad943-56e7-4aab-a480-17f3d1c3bd12 --title '{title}' --prompt '{description}'",
  "project": "nestledjs.com"
}
